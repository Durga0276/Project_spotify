# This is a Databricks asset bundle definition for spotify_proj.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_proj
  uuid: 71a28658-a36e-43dc-8fce-d0b93a1fdf87

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: http://adb-7405609379549575.15.azuredatabricks.net
      root_path: /Workspace/Users/me@azuremaster276gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    presets:
      source_linked_deployment : false
  prod:
    mode: production
    workspace:
      host: http://adb-7405609379549575.15.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/me@azuremaster276gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}

    permissions:
      - user_name: me@azuremaster276gmail.onmicrosoft.com
        level: CAN_MANAGE
